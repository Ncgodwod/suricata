[zeek]
type=standalone
interface=eth0
host=localhost

[logger]
rotation_interval=1d
rotation_size=100M
log_pipe=/var/log/zeek/
type=logger
host=localhost

[manager]
host=localhost

[worker-1]
type=worker
host=localhost
interface=eth0

[worker-2]
type=worker
host=localhost
interface=eth0